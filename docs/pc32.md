# Bounded dynamic R_X86_64_PC32 validation

This branch implements a focused ELF64 x86-64 validator for `R_X86_64_PC32` entries in `DT_RELA`, with GNU `readelf` differential coverage and malformed-input checks.
