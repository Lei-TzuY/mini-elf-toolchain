name: Apply JUMP_SLOT GNU hash slice
on:
  push:
    branches: [feat/jump-slot-gnu-hash]
    paths: [.github/workflows/jump-slot-gnu-hash-apply.yml]
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/jump-slot-gnu-hash
      - name: Apply bounded implementation
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('src/bin/mini-elf-dynrela-jump-slot.rs')
          s = p.read_text()
          s = s.replace('const DT_HASH: i64 = 4;\n', 'const DT_HASH: i64 = 4;\nconst DT_GNU_HASH: i64 = 0x6fff_fef5;\n', 1)
          s = s.replace('    hash: Option<u64>,\n', '    hash: Option<u64>,\n    gnu_hash: Option<u64>,\n', 1)
          s = s.replace('            DT_HASH => Some((&mut d.hash, "DT_HASH")),\n', '            DT_HASH => Some((&mut d.hash, "DT_HASH")),\n            DT_GNU_HASH => Some((&mut d.gnu_hash, "DT_GNU_HASH")),\n', 1)
          old = '''    let hash = req(d.hash, "DT_HASH")?;
    let symtab = req(d.symtab, "DT_SYMTAB")?;
    let syment = req(d.syment, "DT_SYMENT")?;
    let strtab = req(d.strtab, "DT_STRTAB")?;
    let strsz = req(d.strsz, "DT_STRSZ")?;
    if syment != SYMENT as u64 {
        return Err("unsupported DT_SYMENT".into());
    }

    let hash_bytes = map_file(file, &ph, hash, 8, "DT_HASH header")?;
    let symbol_count = u32at(hash_bytes, 4) as u64;
    if symbol_count == 0 {
        return Err("DT_HASH reports zero dynamic symbols".into());
    }
'''
          new = '''    let symtab = req(d.symtab, "DT_SYMTAB")?;
    let syment = req(d.syment, "DT_SYMENT")?;
    let strtab = req(d.strtab, "DT_STRTAB")?;
    let strsz = req(d.strsz, "DT_STRSZ")?;
    if syment != SYMENT as u64 {
        return Err("unsupported DT_SYMENT".into());
    }

    let symbol_count = dynamic_symbol_count(file, &ph, d.hash, d.gnu_hash)?;
    if symbol_count == 0 {
        return Err("dynamic hash metadata reports zero dynamic symbols".into());
    }
'''
          if old not in s:
              raise SystemExit('source anchor not found')
          s = s.replace(old, new, 1)
          marker = 'fn dynstr(tab: &[u8], off: u32, symbol_index: u64) -> Result<String, String> {'
          helper = r'''fn dynamic_symbol_count(
    file: &[u8],
    ph: &[Ph],
    hash: Option<u64>,
    gnu_hash: Option<u64>,
) -> Result<u64, String> {
    if let Some(address) = hash {
        let header = map_file(file, ph, address, 8, "DT_HASH header")?;
        return Ok(u32at(header, 4) as u64);
    }
    let address = gnu_hash.ok_or(
        "R_X86_64_JUMP_SLOT validation requires DT_HASH or DT_GNU_HASH to bound DT_SYMTAB",
    )?;
    gnu_hash_symbol_count(file, ph, address)
}

fn gnu_hash_symbol_count(file: &[u8], ph: &[Ph], address: u64) -> Result<u64, String> {
    let header = map_file(file, ph, address, 16, "DT_GNU_HASH header")?;
    let bucket_count = u32at(header, 0);
    let symbol_offset = u32at(header, 4);
    let bloom_count = u32at(header, 8);
    if bucket_count == 0 {
        return Err("DT_GNU_HASH bucket count must be non-zero".into());
    }
    if bloom_count == 0 || !bloom_count.is_power_of_two() {
        return Err(format!(
            "DT_GNU_HASH bloom count {bloom_count} must be a non-zero power of two"
        ));
    }
    let bloom_bytes = u64::from(bloom_count)
        .checked_mul(8)
        .ok_or("DT_GNU_HASH bloom byte size overflows u64")?;
    let bucket_bytes = u64::from(bucket_count)
        .checked_mul(4)
        .ok_or("DT_GNU_HASH bucket byte size overflows u64")?;
    let prefix_size = 16u64
        .checked_add(bloom_bytes)
        .and_then(|value| value.checked_add(bucket_bytes))
        .ok_or("DT_GNU_HASH prefix byte size overflows u64")?;
    let prefix = map_file(file, ph, address, prefix_size, "DT_GNU_HASH prefix")?;
    let bucket_start = usize::try_from(
        16u64.checked_add(bloom_bytes)
            .ok_or("DT_GNU_HASH bucket offset overflows u64")?,
    )
    .map_err(|_| "DT_GNU_HASH bucket offset does not fit usize")?;
    let chain_address = address
        .checked_add(prefix_size)
        .ok_or("DT_GNU_HASH chain address overflows u64")?;
    let mut count = symbol_offset;
    for bucket_index in 0..bucket_count {
        let off = bucket_start
            .checked_add(
                usize::try_from(u64::from(bucket_index) * 4)
                    .map_err(|_| "DT_GNU_HASH bucket offset does not fit usize")?,
            )
            .ok_or("DT_GNU_HASH bucket offset overflows usize")?;
        let start_symbol = u32at(prefix, off);
        if start_symbol == 0 {
            continue;
        }
        if start_symbol < symbol_offset {
            return Err(format!(
                "DT_GNU_HASH bucket {bucket_index} starts at symbol {start_symbol}, below symbol offset {symbol_offset}"
            ));
        }
        let mut symbol = start_symbol;
        loop {
            let chain_index = symbol
                .checked_sub(symbol_offset)
                .ok_or("DT_GNU_HASH chain index underflows")?;
            let chain_off = u64::from(chain_index)
                .checked_mul(4)
                .ok_or("DT_GNU_HASH chain offset overflows u64")?;
            let entry_address = chain_address
                .checked_add(chain_off)
                .ok_or("DT_GNU_HASH chain address overflows u64")?;
            let entry = map_file(
                file,
                ph,
                entry_address,
                4,
                &format!("DT_GNU_HASH bucket {bucket_index} chain entry for symbol {symbol}"),
            )?;
            let next = symbol
                .checked_add(1)
                .ok_or("DT_GNU_HASH symbol index overflows u32")?;
            if u32at(entry, 0) & 1 != 0 {
                count = count.max(next);
                break;
            }
            symbol = next;
        }
    }
    Ok(u64::from(count))
}

'''
          if marker not in s:
              raise SystemExit('helper insertion anchor not found')
          s = s.replace(marker, helper + marker, 1)
          p.write_text(s)

          t = Path('tests/dynamic_rela_jump_slot.rs')
          s = t.read_text()
          s = s.replace('const DT_HASH: i64 = 4;\n', 'const DT_HASH: i64 = 4;\nconst DT_GNU_HASH: i64 = 0x6fff_fef5;\n', 1)
          append = r'''

fn gnu_hash_fixture(dir: &Path) -> PathBuf {
    let source = dir.join("jump-gnu-hash.s");
    let object = dir.join("jump-gnu-hash.o");
    let shared = dir.join("jump-gnu-hash.so");
    fs::write(
        &source,
        ".text\n.globl call_external\n.type call_external,@function\ncall_external:\ncall external_target@PLT\nret\n",
    )
    .unwrap();
    assert!(Command::new("as")
        .args(["-o", object.to_str().unwrap(), source.to_str().unwrap()])
        .status()
        .unwrap()
        .success());
    assert!(Command::new("ld")
        .args([
            "-shared",
            "--hash-style=gnu",
            "-o",
            shared.to_str().unwrap(),
            object.to_str().unwrap(),
        ])
        .status()
        .unwrap()
        .success());
    shared
}

#[test]
fn validates_gnu_hash_only_jump_slot() {
    let dir = temp("jump-slot-gnu-hash");
    let shared = gnu_hash_fixture(&dir);
    let dynamic = Command::new("readelf")
        .args(["-dW", shared.to_str().unwrap()])
        .output()
        .unwrap();
    assert!(dynamic.status.success());
    let dynamic_text = String::from_utf8_lossy(&dynamic.stdout);
    assert!(dynamic_text.contains("GNU_HASH"), "{dynamic_text}");
    assert!(!dynamic_text.lines().any(|line| line.contains("(HASH)")), "{dynamic_text}");
    let relocs = Command::new("readelf")
        .args(["-rW", shared.to_str().unwrap()])
        .output()
        .unwrap();
    assert!(relocs.status.success());
    assert!(
        String::from_utf8_lossy(&relocs.stdout).contains("R_X86_64_JUMP_SLOT")
    );

    let output = run(&[&shared], "0x70000000");
    assert!(
        output.status.success(),
        "{}",
        String::from_utf8_lossy(&output.stderr)
    );
    assert!(String::from_utf8_lossy(&output.stdout).contains("external_target"));
    fs::remove_dir_all(dir).unwrap();
}

#[test]
fn rejects_malformed_gnu_hash_bloom_count() {
    let dir = temp("jump-slot-gnu-hash-bloom");
    let shared = gnu_hash_fixture(&dir);
    let mut bytes = fs::read(&shared).unwrap();
    let header = map(&bytes, tag(&bytes, DT_GNU_HASH));
    bytes[header + 8..header + 12].copy_from_slice(&0u32.to_le_bytes());
    let bad = dir.join("bad.so");
    fs::write(&bad, bytes).unwrap();
    let output = run(&[&bad], "0");
    assert!(!output.status.success());
    assert!(String::from_utf8_lossy(&output.stderr).contains("bloom count"));
    fs::remove_dir_all(dir).unwrap();
}
'''
          if 'validates_gnu_hash_only_jump_slot' in s:
              raise SystemExit('tests already applied')
          t.write_text(s + append)

          d = Path('docs/mini-elf-dynrela-jump-slot.md')
          s = d.read_text()
          note = '\n## GNU hash compatibility\n\nDynamic-symbol bounds accept SysV `DT_HASH` when present and otherwise derive the bound from a checked GNU `DT_GNU_HASH` table. GNU-hash validation checks non-zero buckets, a non-zero power-of-two bloom count, bucket lower bounds, checked prefix/chain address arithmetic, and file-backed chain termination before `DT_SYMTAB` is sliced. This allows GNU-hash-only shared objects without weakening malformed-input rejection.\n'
          if '## GNU hash compatibility' not in s:
              d.write_text(s.rstrip() + '\n' + note)
          PY
          cargo fmt --all
          cargo clippy --all-targets --all-features -- -D warnings
          cargo test --test dynamic_rela_jump_slot
          cargo test --all-targets --all-features
          git config user.name 'LeiZ'
          git config user.email '52287354+Lei-TzuY@users.noreply.github.com'
          git add src/bin/mini-elf-dynrela-jump-slot.rs tests/dynamic_rela_jump_slot.rs docs/mini-elf-dynrela-jump-slot.md
          git commit -m 'feat: accept GNU-hash-only JUMP_SLOT images'
          git push origin HEAD:feat/jump-slot-gnu-hash
