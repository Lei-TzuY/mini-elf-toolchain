name: PC64 format repair

on:
  push:
    branches:
      - feat/dynrela-pc64-gnu-hash

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/dynrela-pc64-gnu-hash
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Apply canonical rustfmt
        run: cargo fmt --all
      - name: Commit formatting if needed
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "LeiZ"
          git config user.email "52287354+Lei-TzuY@users.noreply.github.com"
          git add src/bin/mini-elf-dynrela-pc64.rs tests/dynamic_rela_pc64_gnu_hash.rs
          git commit -m "style: apply rustfmt to PC64 GNU hash slice"
          git push origin HEAD:feat/dynrela-pc64-gnu-hash
